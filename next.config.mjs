/** @type {import('next').NextConfig} */
const nextConfig = {
    // basePath: '/app',
    experimental: {
        // appDir: true,
        // transpilePackages: ["ui"],
        runtime: "nodejs",
      },
};

export default nextConfig;
